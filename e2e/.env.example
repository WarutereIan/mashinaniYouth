# E2E smoke test credentials (copy to e2e/.env.local — gitignored)

E2E_BASE_URL=http://localhost:8080

# Working superadmin (required for admin + cycle smoke tests)
E2E_ADMIN_EMAIL=superadmin@mashinaniyouth.co.ke
E2E_ADMIN_PASSWORD=ADMIN12345

# Optional: reuse a known voter account instead of creating one each run
# E2E_VOTER_EMAIL=
# E2E_VOTER_PASSWORD=
